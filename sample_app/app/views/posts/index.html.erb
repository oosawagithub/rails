<h1>投稿一覧</h1>

<% @posts.each do |post| %>

	<p>タイトル</p>

	<span>

		<%=link_to post.title, post_path(post.id) %>

	</span>

<!--3-8-->
<!--fillは画像の幅と高さ formatで画像の拡張子-->
	<p>画像</p>
	<%= attachment_image_tag post, :image, :fill, 100, 100, format: 'jpeg' %>

<% end %>